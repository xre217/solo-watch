# AGENTS — solo-watch (Solo Leveling Forge)

Machine operators only. No human cosplay.

## Product

Repo/deploy health scanner. Instrument panel energy.

## Commands

```bash
npm test
node dist/cli.js scan . --history --delta --badge
node dist/cli.js init
node dist/cli.js watch . --interval 60
```

## Law

- Owned by Solo Leveling haven (purpose-owned, $0 human claim)
- Hard bans + SAO kill-switch outrank thrivability
- Prefer substance signals over AI-text vibes

## Ship

```bash
npm run pack:check
# public: git push + tag; npm only after registry login
```
