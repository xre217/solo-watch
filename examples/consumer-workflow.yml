# Drop into any repo as .github/workflows/solo-watch.yml
# Uses local path action once you vendor or submodule solo-watch,
# or switch to a published action after npm/git release.

name: solo-watch

on:
  push:
  pull_request:

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Option A — GitHub (no npm registry):
      # - run: npx --yes github:xre217/solo-watch@v0.8.0 scan . --min-score 55

      # Option B — public composite action:
      - name: Scan with solo-watch action
        uses: xre217/solo-watch@v0.8.0
        with:
          path: "."
          min-score: "55"
          json: "true"

      # Option C — after npm publish:
      # - run: npx --yes solo-watch@0.8.0 scan . --min-score 55
